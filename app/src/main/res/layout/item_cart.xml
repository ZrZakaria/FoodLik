<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:gravity="center_vertical"
    android:orientation="horizontal"
    android:paddingTop="10dp"
    android:paddingBottom="10dp">

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical">

        <TextView
            android:id="@+id/cartItemName"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textColor="@color/foodlik_text"
            android:textSize="16sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/cartItemPrice"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:textColor="@color/foodlik_text_muted"
            android:textSize="14sp" />
    </LinearLayout>

    <ImageButton
        android:id="@+id/decrementCartItem"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:background="@drawable/foodlik_chip"
        android:contentDescription="@string/decrease_quantity"
        android:src="@drawable/ic_remove"
        android:tint="@color/foodlik_text" />

    <TextView
        android:id="@+id/cartItemQuantity"
        android:layout_width="40dp"
        android:layout_height="48dp"
        android:gravity="center"
        android:textColor="@color/foodlik_text"
        android:textSize="16sp"
        android:textStyle="bold" />

    <ImageButton
        android:id="@+id/incrementCartItem"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:background="@drawable/foodlik_chip_selected"
        android:contentDescription="@string/increase_quantity"
        android:src="@drawable/ic_add"
        android:tint="@color/foodlik_text" />

    <ImageButton
        android:id="@+id/removeCartItem"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:layout_marginStart="8dp"
        android:background="@drawable/foodlik_chip"
        android:contentDescription="@string/remove_from_cart"
        android:src="@drawable/ic_delete"
        android:tint="@color/foodlik_error" />
</LinearLayout>
