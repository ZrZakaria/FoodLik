<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/foodlik_background"
    android:orientation="vertical">

    <TextView
        android:id="@+id/connectivityBanner"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/foodlik_error_container"
        android:padding="14dp"
        android:text="@string/connectivity_offline"
        android:textColor="@color/foodlik_on_error_container"
        android:textStyle="bold"
        android:visibility="gone" />

    <FrameLayout
        android:id="@+id/fragmentContainer"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1" />

    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottomNavigation"
        android:layout_width="match_parent"
        android:layout_height="76dp"
        android:background="@color/foodlik_surface_container"
        app:itemActiveIndicatorStyle="@style/Widget.FoodLik.BottomNavigation.ActiveIndicator"
        app:itemHorizontalTranslationEnabled="false"
        app:itemIconSize="24dp"
        app:itemIconTint="@color/foodlik_primary"
        app:itemTextColor="@color/foodlik_text"
        app:labelVisibilityMode="labeled"
        app:menu="@menu/main_nav" />
</LinearLayout>
